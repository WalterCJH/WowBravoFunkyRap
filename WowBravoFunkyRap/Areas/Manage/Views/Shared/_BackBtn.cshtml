@model ButtonModel

<a asp-controller="@Model.ControllerName" asp-action="@Model.ActionName" asp-route-id="@Model.Id" class="btn btn-secondary me-2">
    <i class="fas fa-arrow-left"></i>
    回上頁
</a>
@* 
<a href="#" class="btn btn-secondary me-2" id="js-go-back">
    <i class="fas fa-arrow-left"></i>
    回上頁
</a>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        var referrer = document.referrer;
        var goBackLink = document.getElementById('js-go-back');
        if (goBackLink) {
            goBackLink.href = referrer;
        }
    });
</script> *@